<!-- AUTO-GENERATED by packages/product-model/scripts/generate.py — do not edit -->

# Product model

Every product row keeps a small set of **universal promoted columns** (id,
account, parent product, `kind`, `name`, `slug`, `currency`, `external_ref`,
`current_balance`, `balance_as_of`, `is_active`, `display_order`, timestamps).
Everything kind-specific lives in two validated JSONB payloads:

- **Attributes** — slow-changing identity/config (card last4, maturity dates,
  portfolio). Shallow-merged on write, so a scrape that only saw the dashboard
  can never wipe a field read earlier from a detail page.
- **Metrics** — one per-scrape observation (balance, available, owed, nav,
  units). Appended to snapshot history on change and denormalized as the
  latest value on the product; the writer promotes `metrics.headline()` into
  `current_balance` and stamps `balance_as_of` itself.

Promotion rationale: only universal fields get real columns. No kind-specific
field is aggregated in SQL (all product math lives in JS), so per-kind fields
stay in JSONB typed by this registry rather than widening the table per kind.
Scrapers submit data through the `ScrapedProduct` / `ScrapedTransaction`
envelopes — the generated JSON Schema is the future REST wire contract.

Conventions: snake_case keys; CLP amounts `int`, USD/crypto amounts `float`;
dates as ISO `YYYY-MM-DD` strings; percentages as percent numbers (4.2 = 4.2%).
Optional fields are marked `?`. Every numeric metric field declares a
denomination: `currency` (amount in the product's `currency` column,
convertible), `percent` (raw percent number), or `count` (raw unit count).

## Cuenta corriente (`checking`)

Net-worth role: **asset** · balance convention: **value**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"checking"` | Payload discriminator; always "checking". |
| `account_number` | `str?` | Account number as shown by the institution. |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"checking"` |  | Payload discriminator; always "checking". |
| `balance` | `float` | currency | Available balance in account currency (Saldo disponible). |
| `accounting_balance` | `float?` | currency | Accounting balance in account currency (Saldo contable). |
| `reported_as_of` | `date?` |  | Date the institution printed next to the balance. |

## Ahorro (`savings`)

Net-worth role: **asset** · balance convention: **value**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"savings"` | Payload discriminator; always "savings". |
| `account_number` | `str?` | Account number as shown by the institution. |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"savings"` |  | Payload discriminator; always "savings". |
| `balance` | `float` | currency | Available balance in account currency (Saldo disponible). |
| `accounting_balance` | `float?` | currency | Accounting balance in account currency (Saldo contable). |
| `reported_as_of` | `date?` |  | Date the institution printed next to the balance. |

## Cuenta vista (`vista`)

Net-worth role: **asset** · balance convention: **value**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"vista"` | Payload discriminator; always "vista". |
| `account_number` | `str?` | Account number as shown by the institution. |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"vista"` |  | Payload discriminator; always "vista". |
| `balance` | `float` | currency | Available balance in account currency (Saldo disponible). |
| `accounting_balance` | `float?` | currency | Accounting balance in account currency (Saldo contable). |
| `reported_as_of` | `date?` |  | Date the institution printed next to the balance. |

## Billetera (`wallet`)

Net-worth role: **asset** · balance convention: **value**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"wallet"` | Payload discriminator; always "wallet". |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"wallet"` |  | Payload discriminator; always "wallet". |
| `balance` | `int` | currency | Wallet balance in CLP. |

## Depósito a plazo (`term_deposit`)

Net-worth role: **asset** · balance convention: **value**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"term_deposit"` | Payload discriminator; always "term_deposit". |
| `issue_date` | `date?` | Date the deposit was taken out (Fecha inicio). |
| `maturity_date` | `date?` | Date the deposit matures (Fecha vencimiento). |
| `term_days` | `int?` | Term length in days. |
| `interest_rate_pct` | `float?` | Agreed period rate as a percent number (Tasa período). |
| `deposit_type` | `str?` | Deposit modality (e.g. 'Renovable', 'Plazo fijo'). |
| `principal` | `int?` | Invested principal in CLP (Monto invertido). |
| `maturity_value` | `int?` | Amount payable at maturity in CLP (Monto al vencimiento). |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"term_deposit"` |  | Payload discriminator; always "term_deposit". |
| `balance` | `int` | currency | Current value of the deposit in CLP. |

## Tarjeta de crédito (`credit_card`)

Net-worth role: **liability** · balance convention: **available**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"credit_card"` | Payload discriminator; always "credit_card". |
| `last4` | `str?` | Last four digits of the card number (from the masked '****0000'). |
| `brand` | `str?` | Card network or product brand (e.g. 'Visa Signature'). |
| `statement_day` | `int?` | Day of month the statement closes (facturación). |
| `due_day` | `int?` | Day of month the payment is due (vencimiento). |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"credit_card"` |  | Payload discriminator; always "credit_card". |
| `available` | `float` | currency | Available credit in product currency (Disponible). |
| `limit` | `float?` | currency | Total credit limit in product currency (Cupo total). |
| `owed` | `float?` | currency | Amount drawn as reported by the bank (Utilizado / Monto utilizado). |
| `reported_as_of` | `date?` |  | Date the institution printed next to the card figures. |

## Tarjeta de débito (`debit_card`)

Net-worth role: **none** · balance convention: **none**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"debit_card"` | Payload discriminator; always "debit_card". |
| `last4` | `str?` | Last four digits of the card number (from the masked '****0000'). |
| `brand` | `str?` | Card network or product brand (e.g. 'Visa Signature'). |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"debit_card"` |  | Payload discriminator; always "debit_card". |

## Tarjeta prepago (`prepaid_card`)

Net-worth role: **asset** · balance convention: **value**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"prepaid_card"` | Payload discriminator; always "prepaid_card". |
| `last4` | `str?` | Last four digits of the card number (from the masked '****0000'). |
| `brand` | `str?` | Card network or product brand (e.g. 'Visa Signature'). |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"prepaid_card"` |  | Payload discriminator; always "prepaid_card". |
| `balance` | `int` | currency | Prepaid balance in CLP. |

## Línea de crédito (`line_of_credit`)

Net-worth role: **liability** · balance convention: **available**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"line_of_credit"` | Payload discriminator; always "line_of_credit". |
| `interest_rate_pct` | `float?` | Annual interest rate as a percent number. |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"line_of_credit"` |  | Payload discriminator; always "line_of_credit". |
| `available` | `float` | currency | Available credit in product currency (Disponible). |
| `limit` | `float?` | currency | Total credit limit in product currency (Cupo total). |
| `owed` | `float?` | currency | Amount drawn as reported by the bank (Utilizado / Monto utilizado). |

## Préstamo (`loan`)

Net-worth role: **liability** · balance convention: **owed**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"loan"` | Payload discriminator; always "loan". |
| `original_principal` | `int?` | Original loan principal in CLP. |
| `interest_rate_pct` | `float?` | Annual interest rate as a percent number (e.g. 18.9). |
| `installments_total` | `int?` | Total number of installments (cuotas). |
| `opened_date` | `date?` | Date the loan was granted. |
| `due_day` | `int?` | Day of month each installment is due. |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"loan"` |  | Payload discriminator; always "loan". |
| `owed` | `int` | currency | Outstanding amount owed in CLP. |
| `installments_paid` | `int?` | count | Number of installments already paid. |
| `next_payment_date` | `date?` |  | Due date of the next installment. |
| `next_payment_amount` | `int?` | currency | Amount of the next installment in CLP. |

## Hipotecario (`mortgage`)

Net-worth role: **liability** · balance convention: **owed**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"mortgage"` | Payload discriminator; always "mortgage". |
| `original_principal` | `int?` | Original loan principal in CLP. |
| `interest_rate_pct` | `float?` | Annual interest rate as a percent number (e.g. 18.9). |
| `installments_total` | `int?` | Total number of installments (cuotas). |
| `opened_date` | `date?` | Date the loan was granted. |
| `due_day` | `int?` | Day of month each installment is due. |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"mortgage"` |  | Payload discriminator; always "mortgage". |
| `owed` | `int` | currency | Outstanding amount owed in CLP. |
| `installments_paid` | `int?` | count | Number of installments already paid. |
| `next_payment_date` | `date?` |  | Due date of the next installment. |
| `next_payment_amount` | `int?` | currency | Amount of the next installment in CLP. |

## Inversión (`investment`)

Net-worth role: **asset** · balance convention: **value**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"investment"` | Payload discriminator; always "investment". |
| `portfolio` | `str?` | Portfolio the money is invested in (e.g. 'Risky Norris'). |
| `risk_profile` | `str?` | Risk profile label (e.g. 'Arriesgado'). |
| `fund_name` | `str?` | Fund name for single-fund products (e.g. a fondo mutuo). |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"investment"` |  | Payload discriminator; always "investment". |
| `nav` | `float` | currency | Current market value of the position (net asset value). |
| `deposited` | `float?` | currency | Total amount deposited to date. |
| `profit` | `float?` | currency | Cumulative profit as reported (nav minus deposited). |
| `var_daily_pct` | `float?` | percent | Daily variation as a percent number (Var. diaria). |
| `var_30d_pct` | `float?` | percent | 30-day variation as a percent number (Var. 30 días). |
| `var_ytd_pct` | `float?` | percent | Year-to-date variation as a percent number (Var. año). |

## Cripto (`crypto`)

Net-worth role: **asset** · balance convention: **units**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"crypto"` | Payload discriminator; always "crypto". |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"crypto"` |  | Payload discriminator; always "crypto". |
| `units` | `float` | currency | Coin units held (fractional). |
| `frozen` | `float?` | currency | Units locked in open orders. |
| `pending` | `float?` | currency | Units pending confirmation. |

## Otro (`other`)

Net-worth role: **none** · balance convention: **none**

### Attributes

| Field | Type | Description |
| --- | --- | --- |
| `kind` | `"other"` | Payload discriminator; always "other". |
| `note` | `str?` | Free-form note describing the product. |

### Metrics

| Field | Type | Denomination | Description |
| --- | --- | --- | --- |
| `kind` | `"other"` |  | Payload discriminator; always "other". |
| `balance` | `int?` | currency | Balance in CLP when known. |
